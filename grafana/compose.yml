---
services:
  grafana:
    image: grafana/grafana:latest
    hostname: grafana
    container_name: grafana
    restart: always
    ports:
      - '3000:3000'
    volumes:
      - /opt/volumes/grafana:/var/lib/grafana
